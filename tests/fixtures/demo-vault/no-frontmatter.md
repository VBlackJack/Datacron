# No Frontmatter Here

This note has no YAML header. Title should be resolved from the H1 above.

```python
def hello() -> str:
    return "world"
```

A short paragraph with an inline #code tag and a `#backtick-tag` that must
not be picked up.
